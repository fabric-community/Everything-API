package io.github.fabriccommunity.everything.api.transformer;

public interface QueenTransformer {
    //im far too lazy to actually implement this properly
    int transform(int x, int z, int w, int nw, int n, int ne, int e, int se, int s, int sw, int center);
}
